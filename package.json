{
  "name": "steeb-api-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "vercel-build": "echo 'API-only project - no build step required'",
    "build": "echo 'API-only project - no build step required'"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "firebase-admin": "^13.6.0",
    "mercadopago": "^2.0.11",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^22"
  },
  "engines": {
    "node": "22.x"
  }
}
